export const mergeSortIteratively = (input: number[], length: number): void => {
  if (input.length <= 1) return;

  for (let currentSize = 1; currentSize <= length - 1; currentSize = 2 * currentSize) {

  }


}

const merge = (left: number[], right: number[]): void => {

}